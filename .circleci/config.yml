version: 2

jobs:
  "environmentSetup":
   working_directory: ~/tmp
   docker:
    - image: circleci/node:7.11
   steps:
    - checkout
    - run : npm install
    - run : echo "NPM installed successfully"
   
workflows:
 version: 2
 build:  
  jobs:
   - "environmentSetup"
